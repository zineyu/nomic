# Rust
/target
**/*.rs.bk

# Web UI（web/）
web/node_modules/
web/dist/
web/storybook-static/
web/coverage/
web/*.tsbuildinfo

# devenv
.devenv/
devenv.local.nix
.devenv.flake.nix
.pre-commit-config.yaml

# nix build 输出符号链接
result
result-*

# direnv
.direnv/

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# OS
.DS_Store

# nomic-cli 集成测试以包目录为 cwd 运行真实二进制，bootstrap 会在该目录
# 惰性生成默认 nix 环境（ADR-0041）——属测试副产物，不入库
/crates/app/nomic-cli/.nomic/
